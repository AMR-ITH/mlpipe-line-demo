name: CI Pipeline

on: push

jobs:
  project-testing:
    runs-on: ubuntu-latest

    steps:
      - name: checkout code
        uses: actions/checkout@v3

      - name: setup python
        uses: actions/setup-python@v2
        with:
          python-version: '3.10'

      - name: Cache pip dependencies
        uses: actions/cache@v3
        with:
          path: ~/.cache/pip
          key: ${{ runner.os }}-pip-${{ hashFiles('requirements.txt') }}
          restore-keys: |
            ${{ runner.os }}-pip-

      - name: install dependencies
        run: |
          pip install -r requirements.txt

      - name: run pipeline
        env:
          DAGSHUB_PAT: ${{ secrets.DAGSHUB_PAT }}
        run: |
          dvc repro

      - name: Run model tests
        env:
          DAGSHUB_PAT: ${{ secrets.DAGSHUB_PAT }}
        run: |
          python -m unittest tests/test_model.py

      - name: Promote model to production
        if: success()
        env:
          DAGSHUB_PAT: ${{ secrets.DAGSHUB_PAT }}
        run: python scripts/promote_model.py
      
      - name: Set PYTHONPATH
        run: echo "PYTHONPATH=$PWD/flask_app" >> $GITHUB_ENV

      - name: Run Flask app tests
        if: success()
        env:
          DAGSHUB_PAT: ${{ secrets.DAGSHUB_PAT }}
        run: python -m unittest tests/test_flask_app.py
      
      - name: Reset PYTHONPATH
        run: echo "PYTHONPATH=$(dirname $PWD)" >> $GITHUB_ENV



      # - name: Log in to Docker Hub
      #   if: success()
      #   uses: docker/login-action@v2
      #   with:
      #     username: ${{ secrets.DOCKER_HUB_USERNAME }}
      #     password: ${{ secrets.DOCKER_HUB_ACCESS_TOKEN }}

      # - name: Build Docker image
      #   if: success()
      #   run: |
      #     docker build -t ${{ secrets.DOCKER_HUB_USERNAME }}/emotion3:v1.0 .

      # - name: Push Docker image to Docker Hub
      #   if: success()
      #   run: |
      #     docker push ${{ secrets.DOCKER_HUB_USERNAME }}/emotion3:v1.0

      # - name: Deploy to EC2
      #   if: success()
      #   uses: appleboy/ssh-action@v0.1.5
      #   with:
      #     host: ${{ secrets.EC2_HOST }}
      #     username: ${{ secrets.EC2_USER }}
      #     key: ${{ secrets.EC2_SSH_KEY }}
      #     script: |
      #       docker pull ${{ secrets.DOCKER_HUB_USERNAME }}/test:v1.0
      #       docker stop my-app || true
      #       docker rm my-app || true
      #       docker run -p -d 80:5000 --name my-app \
      #         -e DAGSHUB_PAT=${{ secrets.DAGSHUB_PAT }} \
      #       ${{ secrets.DOCKER_HUB_USERNAME }}/test:v1.0




      # """ ECr to ec2 instance"""
      #  docker file to ecr instance 

      # - name: Configure AWS credentials
      #   if: success()
      #   uses: aws-actions/configure-aws-credentials@v1
      #   with:
      #     aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
      #     aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #     aws-region: ap-south-1

      # - name: Log in to AWS ECR
      #   if: success()
      #   run: |
      #    aws ecr get-login-password --region us-east-1 | docker login --username AWS --password-stdin 116981799876.dkr.ecr.us-east-1.amazonaws.com
      # - name: Build Docker image
      #   if: success()
      #   run: docker build -t yamlguru/repo .

      # - name: Tag Docker image
      #   if: success()
      #   run: docker tag yamlguru/repo:latest 116981799876.dkr.ecr.us-east-1.amazonaws.com/yamlguru/repo:v3.0

      # - name: Push Docker image to AWS ECR
      #   if: success()
      #   run: docker push 116981799876.dkr.ecr.us-east-1.amazonaws.com/yamlguru/repo:v3.0


      # - name: Deploy to EC2
      #   if: success()
      #   uses: appleboy/ssh-action@v0.1.5
      #   with:
      #     host: ${{ secrets.EC2_HOST }}
      #     username: ${{ secrets.EC2_USER }}
      #     key: ${{ secrets.EC2_SSH_KEY }}
      #     script: |
      #       aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
      #       aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
      #       aws configure set region ap-south-1
      #       aws ecr get-login-password --region ap-south-1 | docker login --username AWS --password-stdin 116981799876.dkr.ecr.ap-south-1.amazonaws.com
      #       docker pull 116981799876.dkr.ecr.ap-south-1.amazonaws.com/campusx_ecr:latest
      #       docker stop my-app || true
      #       docker rm my-app || true
      #       docker run -d -p 80:5000 --name my-app \
      #         -e DAGSHUB_PAT=${{ secrets.DAGSHUB_PAT }} \
      #       116981799876.dkr.ecr.ap-south-1.amazonaws.com/campusx_ecr:latest



          # Zip the required files
      - name: Zip files for deployment
        if: success()
        run: |
          zip -r deployment.zip appspec.yml deploy/scripts/install_dependencies.sh deploy/scripts/start_docker.sh

      - name: Configure AWS Credentials
        if: success()
        run: |
          aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws configure set region ap-south-1


        # Upload the ZIP file to S3
      - name: Upload ZIP to S3
        if: success()
        run: |
          aws s3 cp deployment.zip s3://refactored-chainsaw-dvcdb/deployment.zip



        # Deploy to AWS CodeDeploy using the uploaded ZIP file
      - name: Deploy to AWS CodeDeploy
        if: success()
        run: |
          aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws configure set region ap-south-1
          aws deploy create-deployment \
            --application-name MyDockerApp4 \
            --deployment-group-name bluegreen \
            --deployment-config-name CodeDeployDefault.AllAtOnce \
            --s3-location bucket=refactored-chainsaw-dvcdb,key=deployment.zip,bundleType=zip \
            --file-exists-behavior OVERWRITE \
            --region ap-south-1


